[tool.poetry]
name = "hplugins"
version = "0.1.0"
description = ""
authors = ["hikary <hikaryd@proton.me>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
pyprland = "^2.3.8"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

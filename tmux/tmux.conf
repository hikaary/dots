# Плагины
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'omerxx/tmux-floax'
set -g @resurrect-save 'S'
set -g @resurrect-restore 'R'
set -g update-environment "DISPLAY"

# Основные настройки
set -g status-position top
set -g status-justify centre
set -g status-style bg=default
set -g default-terminal "xterm-256color"
set -ga terminal-overrides ",xterm-256color:Tc"
set -g terminal-overrides ',xterm-256color:Tc'
set -as terminal-overrides ',xterm*:sitm=\E[3m'
set -g status-bg default

# Настройки окон
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on

# Форматирование строки состояния
set -g status-left "#[fg=#7aa2f7,bold] tmux "
set -g status-left-length 20
set -g status-right "#[fg=#7aa2f7,bold]#{?client_prefix,⌨️ ,}#{?pane_synchronized,🔄 ,}#(TZ='Asia/Tokyo' date '+%H:%M') "
set -g status-right-length 50

# Стиль неактивных окон
set -g window-status-format "#[fg=#717486,bg=default] #I:#W "

# Стиль активного окна
set -g window-status-current-format "#[fg=#1A1B26,bg=#7aa2f7,bold] #I:#W "

# Клавиша префикса
unbind C-b 
set-option -g prefix C-a
bind C-a send-prefix
# set-option -g prefix C-Space
# bind C-Space send-prefix

# Перезагрузка конфигурации
bind r source-file ~/.config/tmux/tmux.conf \; display "Перезагружено!"

# Разделение панелей
bind v split-window -h -c "#{pane_current_path}"  # Вертикальное разделение

# Изменение размера панелей
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# Переключение между панелями
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Навигация между панелями с помощью Alt + клавиши стрелок
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Изменение размера панелей с помощью Shift + клавиши стрелок
bind -n S-Left resize-pane -L 2
bind -n S-Right resize-pane -R 2
bind -n S-Up resize-pane -U 2
bind -n S-Down resize-pane -D 2

# Быстрое переключение макетов
bind Space next-layout

# Максимизация/минимизация текущей панели
bind z resize-pane -Z

# Закрытие текущей панели
bind x kill-pane

# Создание нового окна с текущим путем
bind c new-window -c "#{pane_current_path}"

# Синхронизация ввода во всех панелях
bind y setw synchronize-panes

# Показать номера панелей
bind q display-pane

# Режим выбора панели/окна
bind P choose-tree

# Быстрое переключение между двумя последними панелями
bind Tab last-pane

# Терминал
set -g @sessionx-bind 'p'

# Перемещение окна
bind-key m command-prompt -p "Переместить окно на позицию: " "run-shell '~/.config/tmux/scripts/move_windows.sh %%'"

# Сохранение и восстановление сессий
bind S run-shell '~/.config/tmux/plugins/tmux-resurrect/scripts/save.sh'
bind R run-shell '~/.config/tmux/plugins/tmux-resurrect/scripts/restore.sh'

run '~/.config/tmux/plugins/tpm/tpm'
